version: 0.0
os: linux
files:
  - source: /
    destination: /opt/myapp
    overwrite: yes

permissions:
  - object: /opt/myapp
    pattern: '**'
    owner: ec2-user
    group: ec2-user
    mode: 755

  - object: /opt/myapp/.env
    owner: ec2-user
    group: ec2-user
    mode: 600

hooks:
  BeforeInstall:
    - location: scripts/stop_server.sh
      timeout: 300
      runas: root

  AfterInstall:
    - location: scripts/install_dependencies.sh
      timeout: 300
      runas: root

  ApplicationStart:
    - location: scripts/start_server.sh
      timeout: 300
      runas: root

  ValidateService:
    - location: scripts/validate_service.sh
      timeout: 300
      runas: root
